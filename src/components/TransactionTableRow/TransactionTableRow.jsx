import styles from './TransactionTableRow.module.css';

const TransactionTableRow = (/*{ item }*/) => {
  return <tr className={styles.TransactionTableRow}>Test row</tr>;
};

export default TransactionTableRow;
